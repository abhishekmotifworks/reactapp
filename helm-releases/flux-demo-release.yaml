apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: fluxcd-demo
  namespace: default
  annotations:
    fluxcd.io/automated: "true"
spec:
  releaseName: fluxcd-demo
  targetNamespace: fluxcd-demo
  chart:
    git: https://github.com/abhishekmotifworks/reactapp
    path: mycharts
    ref: fluxcd-demo
