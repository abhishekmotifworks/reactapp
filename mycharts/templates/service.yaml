apiVersion: v1
kind: Service
metadata:
  name: test-app-service
spec:
  selector:
    app: {{ .Values.appName }}
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: LoadBalancer